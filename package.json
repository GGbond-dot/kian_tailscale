{
  "name": "kian-remote-lab",
  "private": true,
  "version": "0.2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/GGbond-dot/kian_tailscale.git"
  },
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "frontend:dev": "vite --port 1420",
    "frontend:build": "vite build",
    "frontend:check": "node --check src/main.js",
    "lint": "npm run frontend:check && node --check scripts/gpu-node/tcp-bridge.cjs && cargo fmt --manifest-path src-tauri/Cargo.toml -- --check && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml && npm --prefix vscode-extension test",
    "extension:lint": "npm --prefix vscode-extension run lint",
    "extension:package": "npm --prefix vscode-extension run package",
    "verify": "npm run lint && npm run extension:lint && npm run test && npm run frontend:build && npm run extension:package"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0"
  }
}
